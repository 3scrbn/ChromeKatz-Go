It is in the same state as the original, https://github.com/Meckazin/ChromeKatz/tree/main/CredentialKatz, so it works with the same version of Chrome.
I haven't been able to extract the byte pattern for the newer versions in order to dump credentials.
